#!/usr/local/casperscript/bin/bccs -S -sDEVICE=pdfwrite --
(testing) =
(testing again) #
(ARGUMENTS: ) # ARGUMENTS ###
(sys.argv: ) # sys.argv ###
sys.argv 1 get (.pdf) stradd /outfile exch def
<</OutputFile outfile>> setpagedevice
